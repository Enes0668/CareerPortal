@model IEnumerable<Job>
<h2>İş İlanları</h2>
@foreach (var job in Model)
{
    <div>
        <h3>@job.Title</h3>
        <p>@job.Company</p>
        <a asp-action="Details" asp-route-id="@job.Id">Detay</a>
    </div>
}